{
  "name": "managed-deploy-agent",
  "version": "1.0.0",
  "description": "On-server managed deploy agent (Node) with embedded shell toolkit (toolkit/). Public single-clone install for customer app servers.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/EdwardSoaresJr/managed-deploy-agent.git"
  },
  "keywords": [
    "deploy",
    "agent",
    "laravel"
  ]
}
