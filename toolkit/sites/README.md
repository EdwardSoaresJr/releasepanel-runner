# Site environment files

Add one `*.env` per site and environment, for example `sites/my-shop/production.env`.

Keys match the toolkit: `RELEASEPANEL_BASE`, `RELEASEPANEL_REPO`, `DOMAIN`, `RELEASEPANEL_APP_USER`, etc. ReleasePanel can sync these when you register the server and add sites from the control plane.
